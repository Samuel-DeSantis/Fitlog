{
  "name": "fitlog",
  "private": true,
  "version": "2.0.0",
  "description": "Local-first workout tracker. Vanilla HTML/CSS/JS, IndexedDB, no backend.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^30.0.1"
  }
}
