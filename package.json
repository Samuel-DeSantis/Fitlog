{
  "name": "fitlog",
  "private": true,
  "version": "1.0.0",
  "description": "Local-first fitness logbook. Vanilla HTML/CSS/JS, IndexedDB, no backend.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0"
  }
}
